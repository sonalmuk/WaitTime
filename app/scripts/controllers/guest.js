'use strict';

/**
 * @ngdoc function
 * @name waitTimeApp.controller:GuestCtrl
 * @description
 * # GuestCtrl
 * Controller of the waitTimeApp
 */
angular.module('waitTimeApp')
  .controller('GuestCtrl', function ($scope,ProviderService) {
  $scope.designateToQueue=false;     //display button for send

  });
